{
  "name": "db-main-kysely",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "migrate": "tsx -r dotenv/config scripts/migrator.ts",
    "codegen": "kysely-codegen --camel-case --dialect postgres --out-file generated/db.ts",
    "clear": "rm -r dist",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "kysely": "^0.26.3",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/node": "^18.18.7",
    "@types/pg": "^8.10.8",
    "dotenv": "^16.3.1",
    "eslint-config-custom": "workspace:^",
    "kysely-codegen": "^0.11.0",
    "tsconfig": "workspace:^",
    "tsx": "^3.0.0",
    "typescript": "^5.2.2"
  }
}
